# Codex Notes

Keep this project scoped to the Assignment 4 rubric:

- Do not add extra external services unless required.
- Prefer Supabase Auth over Clerk.
- Keep Open-Meteo as the only live data source.
- Keep the worker polling current city weather and upserting `weather_reports`.
- Use the existing SQL in `supabase/schema.sql` as the source of truth for database shape.

